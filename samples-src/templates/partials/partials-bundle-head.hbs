        <!-- Library -->
        <script src="{{ config.baseurl }}/lib/{{ config.bundle }}"></script>
        <!-- Function -->
        <script>
            /** Affichage de la reponse dans la balise textarea (+ console) */
            function print (response) {
                var el = document.getElementById("print");
                el.value = JSON.stringify(response);
                console.log(response);
            };
        </script>
